#include<stdio.h>
#include<string.h>
int main(){
  
    char old_str[] = "old string";
    char new_str[] = "new string";
    strcpy(new_str, old_str); // strcpy function
        puts(new_str);
      
    
    
    return 0; 
}